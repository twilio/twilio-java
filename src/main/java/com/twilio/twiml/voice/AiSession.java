/**
 * This code was generated by
 * \ / _    _  _|   _  _
 *  | (_)\/(_)(_|\/| |(/_  v1.0.0
 *       /       /
 */

package com.twilio.twiml.voice;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonPOJOBuilder;
import com.fasterxml.jackson.dataformat.xml.annotation.JacksonXmlProperty;
import com.twilio.twiml.TwiML;
import com.twilio.twiml.TwiMLException;

import java.util.HashMap;
import java.util.Map;

/**
 * TwiML wrapper for {@code <AiSession>}
 */
@JsonDeserialize(builder = AiSession.Builder.class)
public class AiSession extends TwiML {
    private final String aiConnector;
    private final String aiSessionConfiguration;

    /**
     * For XML Serialization/Deserialization
     */
    private AiSession() {
        this(new Builder());
    }

    /**
     * Create a new {@code <AiSession>} element
     */
    private AiSession(Builder b) {
        super("AiSession", b);
        this.aiConnector = b.aiConnector;
        this.aiSessionConfiguration = b.aiSessionConfiguration;
    }

    /**
     * Attributes to set on the generated XML element
     *
     * @return A Map of attribute keys to values
     */
    protected Map<String, String> getElementAttributes() {
        // Preserve order of attributes
        Map<String, String> attrs = new HashMap<>();

        if (this.getAiConnector() != null) {
            attrs.put("aiConnector", this.getAiConnector());
        }
        if (this.getAiSessionConfiguration() != null) {
            attrs.put("aiSessionConfiguration", this.getAiSessionConfiguration());
        }

        return attrs;
    }

    /**
     * The unique name or installed add-on sid that identifies the installed addon
     * resource for the AI Connector
     *
     * @return The unique name or installed add-on sid that identifies the
     *         installed addon resource for the AI Connector
     */
    public String getAiConnector() {
        return aiConnector;
    }

    /**
     * The unique name or id of the AiSession Configuration resource.
     *
     * @return The unique name or id of the AiSession Configuration resource.
     */
    public String getAiSessionConfiguration() {
        return aiSessionConfiguration;
    }

    /**
     * Create a new {@code <AiSession>} element
     */
    @JsonPOJOBuilder(withPrefix = "")
    public static class Builder extends TwiML.Builder<Builder> {
        /**
         * Create and return a {@code <AiSession.Builder>} from an XML string
         */
        public static Builder fromXml(final String xml) throws TwiMLException {
            try {
                return OBJECT_MAPPER.readValue(xml, Builder.class);
            } catch (final JsonProcessingException jpe) {
                throw new TwiMLException(
                    "Failed to deserialize a AiSession.Builder from the provided XML string: " + jpe.getMessage());
            } catch (final Exception e) {
                throw new TwiMLException("Unhandled exception: " + e.getMessage());
            }
        }

        private String aiConnector;
        private String aiSessionConfiguration;

        /**
         * The unique name or installed add-on sid that identifies the installed addon
         * resource for the AI Connector
         */
        @JacksonXmlProperty(isAttribute = true, localName = "aiConnector")
        public Builder aiConnector(String aiConnector) {
            this.aiConnector = aiConnector;
            return this;
        }

        /**
         * The unique name or id of the AiSession Configuration resource.
         */
        @JacksonXmlProperty(isAttribute = true, localName = "aiSessionConfiguration")
        public Builder aiSessionConfiguration(String aiSessionConfiguration) {
            this.aiSessionConfiguration = aiSessionConfiguration;
            return this;
        }

        /**
         * Create and return resulting {@code <AiSession>} element
         */
        public AiSession build() {
            return new AiSession(this);
        }
    }
}