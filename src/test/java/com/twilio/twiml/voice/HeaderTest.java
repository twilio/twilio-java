/**
 * This code was generated by
 * \ / _    _  _|   _  _
 *  | (_)\/(_)(_|\/| |(/_  v1.0.0
 *       /       /
 */

package com.twilio.twiml.voice;

import com.twilio.twiml.GenericNode;
import org.junit.Assert;
import org.junit.Test;

/**
 * Test class for {@link Header}
 */
public class HeaderTest {
    @Test
    public void testEmptyElement() {
        Header elem = new Header.Builder().build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Header/>",
            elem.toXml()
        );
    }

    @Test
    public void testEmptyElementUrl() {
        Header elem = new Header.Builder().build();

        Assert.assertEquals("%3C%3Fxml+version%3D%221.0%22+encoding%3D%22UTF-8%22%3F%3E%3CHeader%2F%3E", elem.toUrl());
    }

    @Test
    public void testElementWithParams() {
        Header elem = new Header.Builder().name("name").value("value").build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Header name=\"name\" value=\"value\"/>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithExtraAttributes() {
        Header elem = new Header.Builder().option("foo", "bar").option("a", "b").build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Header a=\"b\" foo=\"bar\"/>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithTextNode() {
        Header.Builder builder = new Header.Builder();

        builder.addText("Hey no tags!");

        Header elem = builder.build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Header>" +
            "Hey no tags!" +
            "</Header>",
            elem.toXml()
        );
    }

    @Test
    public void testMixedContent() {
        GenericNode.Builder child = new GenericNode.Builder("Child");
        child.addText("content");

        Header.Builder builder = new Header.Builder();

        builder.addText("before");
        builder.addChild(child.build());
        builder.addText("after");

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Header>" +
            "before" +
            "<Child>content</Child>" +
            "after" +
            "</Header>",
            builder.build().toXml()
        );
    }

    @Test
    public void testElementWithGenericNode() {
        GenericNode.Builder genericBuilder = new GenericNode.Builder("genericTag");
        genericBuilder.addText("Some text");
        GenericNode node = genericBuilder.build();

        Header.Builder builder = new Header.Builder();
        Header elem = builder.addChild(node).build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Header>" +
            "<genericTag>" +
            "Some text" +
            "</genericTag>" +
            "</Header>",
            elem.toXml()
        );
    }

    @Test
    public void testXmlAttributesDeserialization() {
        final Header elem = new Header.Builder().name("name").value("value").build();

        Assert.assertEquals(
            Header.Builder.fromXml("<Header name=\"name\" value=\"value\"/>").build().toXml(),
            elem.toXml()
        );
    }
}