/**
 * This code was generated by
 * \ / _    _  _|   _  _
 *  | (_)\/(_)(_|\/| |(/_  v1.0.0
 *       /       /
 */

package com.twilio.twiml.voice;

import com.twilio.converter.Promoter;
import com.twilio.http.HttpMethod;
import org.junit.Assert;
import org.junit.Test;

import java.net.URI;
import java.util.List;

/**
 * Test class for {@link WhatsApp}
 */
public class WhatsAppTest {
    @Test
    public void testElementWithParams() {
        WhatsApp elem = new WhatsApp.Builder(new com.twilio.type.PhoneNumber("+15017122661"))
            .url(URI.create("https://example.com"))
            .method(HttpMethod.GET)
            .statusCallbackEvents(Promoter.listOfOne(WhatsApp.Event.INITIATED))
            .statusCallback(URI.create("https://example.com"))
            .statusCallbackMethod(HttpMethod.GET)
            .build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<WhatsApp method=\"GET\" statusCallback=\"https://example.com\" statusCallbackEvent=\"initiated\" statusCallbackMethod=\"GET\" url=\"https://example.com\">+15017122661</WhatsApp>",
            elem.toXml()
        );
    }

    @Test
    public void testXmlAttributesDeserialization() {
        final WhatsApp elem = new WhatsApp.Builder(new com.twilio.type.PhoneNumber("+15017122661"))
            .url(URI.create("https://example.com"))
            .method(HttpMethod.GET)
            .statusCallbackEvents(Promoter.listOfOne(WhatsApp.Event.INITIATED))
            .statusCallback(URI.create("https://example.com"))
            .statusCallbackMethod(HttpMethod.GET)
            .build();

        Assert.assertEquals(
            WhatsApp.Builder.fromXml("<WhatsApp method=\"GET\" statusCallback=\"https://example.com\" statusCallbackEvent=\"initiated\" statusCallbackMethod=\"GET\" url=\"https://example.com\">+15017122661</WhatsApp>").build().toXml(),
            elem.toXml()
        );
    }
}