/*
 * This code was generated by
 * ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
 *  |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
 *  |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \
 *
 * Twilio Memory API
 * APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.
 *
 * NOTE: This class is auto generated by OpenAPI Generator.
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package com.twilio.rest.memory.v1;

import com.twilio.base.Page;
import com.twilio.base.Reader;
import com.twilio.base.ResourceSet;
import com.twilio.base.ResourceSetResponse;
import com.twilio.base.TokenPaginationPage;
import com.twilio.base.TwilioResponse;
import com.twilio.constant.EnumConstants.ParameterType;
import com.twilio.converter.Serializer;
import com.twilio.exception.ApiConnectionException;
import com.twilio.exception.ApiException;
import com.twilio.exception.RestException;
import com.twilio.exception.RestStandardException;
import com.twilio.http.HttpMethod;
import com.twilio.http.HttpUtility;
import com.twilio.http.Request;
import com.twilio.http.Response;
import com.twilio.http.TwilioRestClient;
import com.twilio.rest.Domains;
import com.twilio.type.*;
import java.io.InputStream;
import java.time.ZonedDateTime;

public class ObservationReader
    extends Reader<Observation.ListObservationResponse> {

    private String pathStoreId;
    private String pathProfileId;
    private Integer pageSize;
    private String pageToken;
    private Observation.OrderBy orderBy;
    private String source;
    private ZonedDateTime createdAfter;
    private ZonedDateTime createdBefore;
    private String acceptEncoding;

    public ObservationReader(
        final String pathStoreId,
        final String pathProfileId
    ) {
        this.pathStoreId = pathStoreId;
        this.pathProfileId = pathProfileId;
    }

    public ObservationReader setPageSize(final Integer pageSize) {
        this.pageSize = pageSize;
        return this;
    }

    public ObservationReader setPageToken(final String pageToken) {
        this.pageToken = pageToken;
        return this;
    }

    public ObservationReader setOrderBy(final Observation.OrderBy orderBy) {
        this.orderBy = orderBy;
        return this;
    }

    public ObservationReader setSource(final String source) {
        this.source = source;
        return this;
    }

    public ObservationReader setCreatedAfter(final ZonedDateTime createdAfter) {
        this.createdAfter = createdAfter;
        return this;
    }

    public ObservationReader setCreatedBefore(
        final ZonedDateTime createdBefore
    ) {
        this.createdBefore = createdBefore;
        return this;
    }

    public ObservationReader setAcceptEncoding(final String acceptEncoding) {
        this.acceptEncoding = acceptEncoding;
        return this;
    }

    public ResourceSetResponse<
        Observation.ListObservationResponse
    > readWithResponse(final TwilioRestClient client) {
        Request request = buildFirstPageRequest(client);
        Response response = makeRequest(client, request);
        Page<Observation.ListObservationResponse> page = Page.fromJson(
            "observations",
            response.getContent(),
            Observation.ListObservationResponse.class,
            client.getObjectMapper()
        );
        ResourceSet<Observation.ListObservationResponse> resourceSet =
            new ResourceSet<>(this, client, page);
        return new ResourceSetResponse<>(
            resourceSet,
            response.getStatusCode(),
            response.getHeaders()
        );
    }

    private Request buildFirstPageRequest(final TwilioRestClient client) {
        String path = "/v1/Stores/{storeId}/Profiles/{profileId}/Observations";

        path = path.replace("{" + "storeId" + "}", this.pathStoreId.toString());
        path =
            path.replace(
                "{" + "profileId" + "}",
                this.pathProfileId.toString()
            );

        Request request = new Request(
            HttpMethod.GET,
            Domains.MEMORY.toString(),
            path
        );
        addQueryParams(request);
        addHeaderParams(request);
        return request;
    }

    @Override
    public ResourceSet<Observation.ListObservationResponse> read(
        final TwilioRestClient client
    ) {
        return new ResourceSet<>(this, client, firstPage(client));
    }

    public Page<Observation.ListObservationResponse> firstPage(
        final TwilioRestClient client
    ) {
        Request request = buildFirstPageRequest(client);
        return pageForRequest(client, request);
    }

    public TwilioResponse<
        Page<Observation.ListObservationResponse>
    > firstPageWithResponse(final TwilioRestClient client) {
        Request request = buildFirstPageRequest(client);
        Response response = makeRequest(client, request);
        Page<Observation.ListObservationResponse> page = Page.fromJson(
            "observations",
            response.getContent(),
            Observation.ListObservationResponse.class,
            client.getObjectMapper()
        );
        return new TwilioResponse<>(
            page,
            response.getStatusCode(),
            response.getHeaders()
        );
    }

    private Response makeRequest(
        final TwilioRestClient client,
        final Request request
    ) {
        Response response = client.request(request);
        if (response == null) {
            throw new ApiConnectionException(
                "Observation read failed: Unable to connect to server"
            );
        } else if (!TwilioRestClient.SUCCESS.test(response.getStatusCode())) {
            InputStream inputStream = response.getStream();
            RestStandardException standardException =
                RestStandardException.fromJson(
                    inputStream,
                    client.getObjectMapper()
                );
            if (
                standardException != null && standardException.getType() != null
            ) {
                throw new ApiException(standardException);
            }
            RestException restException = RestException.fromJson(
                inputStream,
                client.getObjectMapper()
            );

            if (restException == null) {
                throw new ApiException(
                    "Server Error, no content",
                    response.getStatusCode()
                );
            }
            throw new ApiException(restException);
        }
        return response;
    }

    private Page<Observation.ListObservationResponse> pageForRequest(
        final TwilioRestClient client,
        final Request request
    ) {
        Response response = makeRequest(client, request);
        return TokenPaginationPage.fromJson(
            "observations",
            response.getContent(),
            Observation.ListObservationResponse.class,
            client.getObjectMapper()
        );
    }

    @Override
    public Page<Observation.ListObservationResponse> previousPage(
        final Page<Observation.ListObservationResponse> page,
        final TwilioRestClient client
    ) {
        String path = "/v1/Stores/{storeId}/Profiles/{profileId}/Observations";

        path = path.replace("{" + "storeId" + "}", this.pathStoreId.toString());
        path =
            path.replace(
                "{" + "profileId" + "}",
                this.pathProfileId.toString()
            );

        path = path + page.previousQueryString();
        Request request = new Request(
            HttpMethod.GET,
            Domains.MEMORY.toString(),
            path
        );

        return pageForRequest(client, request);
    }

    @Override
    public Page<Observation.ListObservationResponse> nextPage(
        final Page<Observation.ListObservationResponse> page,
        final TwilioRestClient client
    ) {
        String path = "/v1/Stores/{storeId}/Profiles/{profileId}/Observations";

        path = path.replace("{" + "storeId" + "}", this.pathStoreId.toString());
        path =
            path.replace(
                "{" + "profileId" + "}",
                this.pathProfileId.toString()
            );

        path = path + page.nextQueryString();
        Request request = new Request(
            HttpMethod.GET,
            Domains.MEMORY.toString(),
            path
        );

        return pageForRequest(client, request);
    }

    @Override
    public Page<Observation.ListObservationResponse> getPage(
        final String targetUrl,
        final TwilioRestClient client
    ) {
        if (!com.twilio.http.HttpUtility.isValidTwilioUrl(targetUrl)) {
            throw new ApiException(
                "Invalid URL: URL must be a valid Twilio domain"
            );
        }
        Request request = new Request(HttpMethod.GET, targetUrl);
        return pageForRequest(client, request);
    }

    private void addQueryParams(final Request request) {
        if (pageSize != null) {
            Serializer.toString(
                request,
                "pageSize",
                pageSize,
                ParameterType.QUERY
            );
        }

        if (pageToken != null) {
            Serializer.toString(
                request,
                "pageToken",
                pageToken,
                ParameterType.QUERY
            );
        }

        if (orderBy != null) {
            Serializer.toString(
                request,
                "orderBy",
                orderBy,
                ParameterType.QUERY
            );
        }

        if (source != null) {
            Serializer.toString(request, "source", source, ParameterType.QUERY);
        }

        if (createdAfter != null) {
            Serializer.toString(
                request,
                "createdAfter",
                createdAfter,
                ParameterType.QUERY
            );
        }

        if (createdBefore != null) {
            Serializer.toString(
                request,
                "createdBefore",
                createdBefore,
                ParameterType.QUERY
            );
        }

        if (getPageSize() != null) {
            request.addQueryParam("PageSize", Integer.toString(getPageSize()));
        }
    }

    private void addHeaderParams(final Request request) {
        if (acceptEncoding != null) {
            Serializer.toString(
                request,
                "Accept-Encoding",
                acceptEncoding,
                ParameterType.HEADER
            );
        }
    }
}
