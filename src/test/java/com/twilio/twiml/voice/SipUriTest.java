/**
 * This code was generated by
 * \ / _    _  _|   _  _
 *  | (_)\/(_)(_|\/| |(/_  v1.0.0
 *       /       /
 */

package com.twilio.twiml.voice;

import org.junit.Assert;
import org.junit.Test;

import java.net.URI;

/**
 * Test class for {@link SipUri}
 */
public class SipUriTest {
    @Test
    public void testElementWithParams() {
        SipUri elem = new SipUri.Builder(URI.create("https://example.com"))
            .priority("priority")
            .weight("weight")
            .username("username")
            .password("password")
            .build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Uri password=\"password\" priority=\"priority\" username=\"username\" weight=\"weight\">https://example.com</Uri>",
            elem.toXml()
        );
    }

    @Test
    public void testXmlAttributesDeserialization() {
        final SipUri elem = new SipUri.Builder(URI.create("https://example.com"))
            .priority("priority")
            .weight("weight")
            .username("username")
            .password("password")
            .build();

        Assert.assertEquals(
            SipUri.Builder.fromXml("<Uri password=\"password\" priority=\"priority\" username=\"username\" weight=\"weight\">https://example.com</Uri>").build().toXml(),
            elem.toXml()
        );
    }
}