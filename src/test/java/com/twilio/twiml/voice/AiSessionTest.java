/**
 * This code was generated by
 * \ / _    _  _|   _  _
 *  | (_)\/(_)(_|\/| |(/_  v1.0.0
 *       /       /
 */

package com.twilio.twiml.voice;

import com.twilio.twiml.GenericNode;
import org.junit.Assert;
import org.junit.Test;

/**
 * Test class for {@link AiSession}
 */
public class AiSessionTest {
    @Test
    public void testEmptyElement() {
        AiSession elem = new AiSession.Builder().build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<AiSession/>",
            elem.toXml()
        );
    }

    @Test
    public void testEmptyElementUrl() {
        AiSession elem = new AiSession.Builder().build();

        Assert.assertEquals("%3C%3Fxml+version%3D%221.0%22+encoding%3D%22UTF-8%22%3F%3E%3CAiSession%2F%3E", elem.toUrl());
    }

    @Test
    public void testElementWithParams() {
        AiSession elem = new AiSession.Builder()
            .aiConnector("ai_connector")
            .aiSessionConfiguration("ai_session_configuration")
            .build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<AiSession aiConnector=\"ai_connector\" aiSessionConfiguration=\"ai_session_configuration\"/>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithExtraAttributes() {
        AiSession elem = new AiSession.Builder().option("foo", "bar").option("a", "b").build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<AiSession a=\"b\" foo=\"bar\"/>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithTextNode() {
        AiSession.Builder builder = new AiSession.Builder();

        builder.addText("Hey no tags!");

        AiSession elem = builder.build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<AiSession>" +
            "Hey no tags!" +
            "</AiSession>",
            elem.toXml()
        );
    }

    @Test
    public void testMixedContent() {
        GenericNode.Builder child = new GenericNode.Builder("Child");
        child.addText("content");

        AiSession.Builder builder = new AiSession.Builder();

        builder.addText("before");
        builder.addChild(child.build());
        builder.addText("after");

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<AiSession>" +
            "before" +
            "<Child>content</Child>" +
            "after" +
            "</AiSession>",
            builder.build().toXml()
        );
    }

    @Test
    public void testElementWithGenericNode() {
        GenericNode.Builder genericBuilder = new GenericNode.Builder("genericTag");
        genericBuilder.addText("Some text");
        GenericNode node = genericBuilder.build();

        AiSession.Builder builder = new AiSession.Builder();
        AiSession elem = builder.addChild(node).build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<AiSession>" +
            "<genericTag>" +
            "Some text" +
            "</genericTag>" +
            "</AiSession>",
            elem.toXml()
        );
    }

    @Test
    public void testXmlAttributesDeserialization() {
        final AiSession elem = new AiSession.Builder()
            .aiConnector("ai_connector")
            .aiSessionConfiguration("ai_session_configuration")
            .build();

        Assert.assertEquals(
            AiSession.Builder.fromXml("<AiSession aiConnector=\"ai_connector\" aiSessionConfiguration=\"ai_session_configuration\"/>").build().toXml(),
            elem.toXml()
        );
    }
}