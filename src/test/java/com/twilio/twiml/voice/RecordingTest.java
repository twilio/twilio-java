/**
 * This code was generated by
 * \ / _    _  _|   _  _
 *  | (_)\/(_)(_|\/| |(/_  v1.0.0
 *       /       /
 */

package com.twilio.twiml.voice;

import com.twilio.converter.Promoter;
import com.twilio.twiml.GenericNode;
import org.junit.Assert;
import org.junit.Test;

import java.util.List;

/**
 * Test class for {@link Recording}
 */
public class RecordingTest {
    @Test
    public void testEmptyElement() {
        Recording elem = new Recording.Builder().build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Recording/>",
            elem.toXml()
        );
    }

    @Test
    public void testEmptyElementUrl() {
        Recording elem = new Recording.Builder().build();

        Assert.assertEquals("%3C%3Fxml+version%3D%221.0%22+encoding%3D%22UTF-8%22%3F%3E%3CRecording%2F%3E", elem.toUrl());
    }

    @Test
    public void testElementWithParams() {
        Recording elem = new Recording.Builder()
            .recordingStatusCallback("recording_status_callback")
            .recordingStatusCallbackMethod(Recording.RecordingStatusCallbackMethod.GET)
            .recordingStatusCallbackEvents(Promoter.listOfOne(Recording.Event.IN_PROGRESS))
            .trim(Recording.Trim.TRIM_SILENCE)
            .track(Recording.Track.INBOUND)
            .channels(Recording.Channels.MONO)
            .build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Recording channels=\"mono\" recordingStatusCallback=\"recording_status_callback\" recordingStatusCallbackEvent=\"in-progress\" recordingStatusCallbackMethod=\"GET\" track=\"inbound\" trim=\"trim-silence\"/>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithExtraAttributes() {
        Recording elem = new Recording.Builder().option("foo", "bar").option("a", "b").build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Recording a=\"b\" foo=\"bar\"/>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithTextNode() {
        Recording.Builder builder = new Recording.Builder();

        builder.addText("Hey no tags!");

        Recording elem = builder.build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Recording>" +
            "Hey no tags!" +
            "</Recording>",
            elem.toXml()
        );
    }

    @Test
    public void testMixedContent() {
        GenericNode.Builder child = new GenericNode.Builder("Child");
        child.addText("content");

        Recording.Builder builder = new Recording.Builder();

        builder.addText("before");
        builder.addChild(child.build());
        builder.addText("after");

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Recording>" +
            "before" +
            "<Child>content</Child>" +
            "after" +
            "</Recording>",
            builder.build().toXml()
        );
    }

    @Test
    public void testElementWithGenericNode() {
        GenericNode.Builder genericBuilder = new GenericNode.Builder("genericTag");
        genericBuilder.addText("Some text");
        GenericNode node = genericBuilder.build();

        Recording.Builder builder = new Recording.Builder();
        Recording elem = builder.addChild(node).build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Recording>" +
            "<genericTag>" +
            "Some text" +
            "</genericTag>" +
            "</Recording>",
            elem.toXml()
        );
    }

    @Test
    public void testXmlAttributesDeserialization() {
        final Recording elem = new Recording.Builder()
            .recordingStatusCallback("recording_status_callback")
            .recordingStatusCallbackMethod(Recording.RecordingStatusCallbackMethod.GET)
            .recordingStatusCallbackEvents(Promoter.listOfOne(Recording.Event.IN_PROGRESS))
            .trim(Recording.Trim.TRIM_SILENCE)
            .track(Recording.Track.INBOUND)
            .channels(Recording.Channels.MONO)
            .build();

        Assert.assertEquals(
            Recording.Builder.fromXml("<Recording channels=\"mono\" recordingStatusCallback=\"recording_status_callback\" recordingStatusCallbackEvent=\"in-progress\" recordingStatusCallbackMethod=\"GET\" track=\"inbound\" trim=\"trim-silence\"/>").build().toXml(),
            elem.toXml()
        );
    }
}