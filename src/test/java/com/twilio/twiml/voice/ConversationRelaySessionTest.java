/**
 * This code was generated by
 * \ / _    _  _|   _  _
 *  | (_)\/(_)(_|\/| |(/_  v1.0.0
 *       /       /
 */

package com.twilio.twiml.voice;

import com.twilio.twiml.GenericNode;
import org.junit.Assert;
import org.junit.Test;

/**
 * Test class for {@link ConversationRelaySession}
 */
public class ConversationRelaySessionTest {
    @Test
    public void testEmptyElement() {
        ConversationRelaySession elem = new ConversationRelaySession.Builder().build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<ConversationRelaySession/>",
            elem.toXml()
        );
    }

    @Test
    public void testEmptyElementUrl() {
        ConversationRelaySession elem = new ConversationRelaySession.Builder().build();

        Assert.assertEquals("%3C%3Fxml+version%3D%221.0%22+encoding%3D%22UTF-8%22%3F%3E%3CConversationRelaySession%2F%3E", elem.toUrl());
    }

    @Test
    public void testElementWithParams() {
        ConversationRelaySession elem = new ConversationRelaySession.Builder()
            .connector("connector")
            .sessionConfiguration("session_configuration")
            .build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<ConversationRelaySession connector=\"connector\" sessionConfiguration=\"session_configuration\"/>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithExtraAttributes() {
        ConversationRelaySession elem = new ConversationRelaySession.Builder().option("foo", "bar").option("a", "b").build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<ConversationRelaySession a=\"b\" foo=\"bar\"/>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithTextNode() {
        ConversationRelaySession.Builder builder = new ConversationRelaySession.Builder();

        builder.addText("Hey no tags!");

        ConversationRelaySession elem = builder.build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<ConversationRelaySession>" +
            "Hey no tags!" +
            "</ConversationRelaySession>",
            elem.toXml()
        );
    }

    @Test
    public void testMixedContent() {
        GenericNode.Builder child = new GenericNode.Builder("Child");
        child.addText("content");

        ConversationRelaySession.Builder builder = new ConversationRelaySession.Builder();

        builder.addText("before");
        builder.addChild(child.build());
        builder.addText("after");

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<ConversationRelaySession>" +
            "before" +
            "<Child>content</Child>" +
            "after" +
            "</ConversationRelaySession>",
            builder.build().toXml()
        );
    }

    @Test
    public void testElementWithGenericNode() {
        GenericNode.Builder genericBuilder = new GenericNode.Builder("genericTag");
        genericBuilder.addText("Some text");
        GenericNode node = genericBuilder.build();

        ConversationRelaySession.Builder builder = new ConversationRelaySession.Builder();
        ConversationRelaySession elem = builder.addChild(node).build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<ConversationRelaySession>" +
            "<genericTag>" +
            "Some text" +
            "</genericTag>" +
            "</ConversationRelaySession>",
            elem.toXml()
        );
    }

    @Test
    public void testXmlAttributesDeserialization() {
        final ConversationRelaySession elem = new ConversationRelaySession.Builder()
            .connector("connector")
            .sessionConfiguration("session_configuration")
            .build();

        Assert.assertEquals(
            ConversationRelaySession.Builder.fromXml("<ConversationRelaySession connector=\"connector\" sessionConfiguration=\"session_configuration\"/>").build().toXml(),
            elem.toXml()
        );
    }
}