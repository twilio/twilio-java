/**
 * This code was generated by
 * \ / _    _  _|   _  _
 *  | (_)\/(_)(_|\/| |(/_  v1.0.0
 *       /       /
 */

package com.twilio.twiml.voice;

import com.twilio.twiml.GenericNode;
import org.junit.Assert;
import org.junit.Test;

/**
 * Test class for {@link Headers}
 */
public class HeadersTest {
    @Test
    public void testEmptyElement() {
        Headers elem = new Headers.Builder().build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Headers/>",
            elem.toXml()
        );
    }

    @Test
    public void testEmptyElementUrl() {
        Headers elem = new Headers.Builder().build();

        Assert.assertEquals("%3C%3Fxml+version%3D%221.0%22+encoding%3D%22UTF-8%22%3F%3E%3CHeaders%2F%3E", elem.toUrl());
    }

    @Test
    public void testElementWithExtraAttributes() {
        Headers elem = new Headers.Builder().option("foo", "bar").option("a", "b").build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Headers a=\"b\" foo=\"bar\"/>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithChildren() {
        Headers.Builder builder = new Headers.Builder();

        builder.header(new Header.Builder().name("name").value("value").build());

        Headers elem = builder.build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Headers>" +
                "<Header name=\"name\" value=\"value\"/>" +
            "</Headers>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithTextNode() {
        Headers.Builder builder = new Headers.Builder();

        builder.addText("Hey no tags!");

        Headers elem = builder.build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Headers>" +
            "Hey no tags!" +
            "</Headers>",
            elem.toXml()
        );
    }

    @Test
    public void testMixedContent() {
        GenericNode.Builder child = new GenericNode.Builder("Child");
        child.addText("content");

        Headers.Builder builder = new Headers.Builder();

        builder.addText("before");
        builder.addChild(child.build());
        builder.addText("after");

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Headers>" +
            "before" +
            "<Child>content</Child>" +
            "after" +
            "</Headers>",
            builder.build().toXml()
        );
    }

    @Test
    public void testElementWithGenericNode() {
        GenericNode.Builder genericBuilder = new GenericNode.Builder("genericTag");
        genericBuilder.addText("Some text");
        GenericNode node = genericBuilder.build();

        Headers.Builder builder = new Headers.Builder();
        Headers elem = builder.addChild(node).build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Headers>" +
            "<genericTag>" +
            "Some text" +
            "</genericTag>" +
            "</Headers>",
            elem.toXml()
        );
    }

    @Test
    public void testElementWithGenericNodeAttributes() {
        GenericNode.Builder genericBuilder = new GenericNode.Builder("genericTag");
        GenericNode node = genericBuilder.option("key", "value").addText("someText").build();

        Headers.Builder builder = new Headers.Builder();
        Headers elem = builder.addChild(node).build();

        Assert.assertEquals(
            "<?xml version=\"1.0\" encoding=\"UTF-8\"?>" +
            "<Headers>" +
            "<genericTag key=\"value\">" +
            "someText" +
            "</genericTag>" +
            "</Headers>",
            elem.toXml()
        );
    }

    @Test
    public void testXmlAttributesDeserialization() {
        final Headers elem = new Headers.Builder().build();

        Assert.assertEquals(
            Headers.Builder.fromXml("<Headers/>").build().toXml(),
            elem.toXml()
        );
    }

    @Test
    public void testXmlChildrenDeserialization() {
        final Headers.Builder builder = new Headers.Builder();

        builder.header(new Header.Builder().name("name").value("value").build());

        final Headers elem = builder.build();

        Assert.assertEquals(
            Headers.Builder.fromXml("<Headers>" +
                "<Header name=\"name\" value=\"value\"/>" +
            "</Headers>").build().toXml(),
            elem.toXml()
        );
    }

    @Test
    public void testXmlEmptyChildrenDeserialization() {
        final Headers.Builder builder = new Headers.Builder();

        builder.header(new Header.Builder().build());

        final Headers elem = builder.build();

        Assert.assertEquals(
            Headers.Builder.fromXml("<Headers>" +
                "<Header/>" +
            "</Headers>").build().toXml(),
            elem.toXml()
        );
    }
}